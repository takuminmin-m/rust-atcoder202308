use proconio::{ input, marker::Chars };

fn main() {
    input! {
        n: usize,
        m: usize,
        k: usize,
    }

    let mut g = vec![vec![0; 0]; n];
    for _ in 0..n {
        input! {
            a: usize,
            b: usize,
            l: usize,
            c: usize,
        }

        
    }
}
